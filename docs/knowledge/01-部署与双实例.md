---
title: 部署与双实例
tags: [看板, 部署, 运维]
date: 2026-08-21
status: 已确认
related: [[00-知识地图(MOC)], [02-demo正式版机制]]
---

# 部署与双实例

多项目管理个人看板的本地运行与双实例架构。公网演示版（脱敏）与本机正式版（真实数据）**同时运行**，互不干扰。

## 启动命令

```bash
npm install
node server.js          # 正式版：端口 5181（真实数据，写操作需令牌 data/auth.token）
node server.js --demo   # 演示版：端口 5180（脱敏数据 + 免令牌，公网隧道指向它）
node watchdog.js        # 守护双实例：5180 演示 + 5181 正式，崩溃各自自动拉起 + 维护 Cloudflare 隧道
```

## 端口约定（勿改）

| 端口 | 版本 | 数据 | 令牌 | 用途 |
|---|---|---|---|---|
| 5180 | 演示版（`--demo`） | 脱敏 | 免令牌 | 公网评委用 |
| 5181 | 正式版（真实数据） | 真实 | 需 `data/auth.token` | 本机日常用 |

- 公网网址：每次重启变化，读 `data/tunnel-url.txt`（**始终指向 5180 演示版**，避免暴露真实项目名）。
- 重启流程：`taskkill /IM node.exe /F` → `node watchdog.js`（自动拉起双实例）。

## 共享文件注意

`templates.json` 为两实例**共享**文件：demo 评委在公网增删改模板会同步影响正式版模板列表（项目数据互不影响）。若介意，后续可把模板也按 demo/正式拆开。

## 延伸

- 双实例参数分工见 [[02-demo正式版机制]]
- 业务规则见 [[03-看板业务规则]]

## 隧道模式（2026-08-25 恢复本机模式）

- 曾计划隧道迁至 NAS（systemd cloudflared.service）；因 NAS 暂不支持 Ubuntu VM，**恢复本机跑隧道**（watchdog 守护）。
- watchdog 隧道守护 = **公网健康探测**（https.get kanban.forestkopa.top，6s 超时）+ 不可达时 taskkill 残留后重启 cloudflared（`tunnel --config config.yml run`）。
- 教训：**进程存在 ≠ 隧道健康**——曾残留僵死 cloudflared.exe 骗过"进程检测"导致公网 530；故改用公网探测。
- cloudflared 位置：`C:/Users/Administrator/.cloudflared/cloudflared.exe`（v2026.8.2）；config.yml 在项目根（ingress 指向 5180）。

## 开机自启：NSSM 服务方式（2026-08-25 定案）

- 服务名 `kanban-watchdog`（NSSM 包装 node watchdog.js，开机自启、无需登录、崩溃 5s 自动重启）。
- 安装：管理员运行 `install-watchdog-service.bat`（**CRLF+ASCII 编码**，绝对路径调用 nssm，开头 stop/remove 容错清理残留）。
- 教训：bat 必须 CRLF+ASCII（UTF-8 无 BOM 会被 cmd 当 GBK 解析导致全命令乱码）；不要用 set 变量+引号嵌套。
- 运维：`nssm status/start/stop/restart kanban-watchdog`；日志 `data/watchdog-service.log`（1MB 轮转）；卸载 `nssm remove kanban-watchdog confirm`（需管理员）。
- 曾试启动文件夹 vbs 自启——本机环境拦截间接启动进程，**弃用**（kanban-watchdog.vbs 已从启动文件夹移除）。
